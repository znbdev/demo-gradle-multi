// 子项目demo-core的build.gradle文件

// 特定于Spring Boot的配置
springBoot {
    // 主类名
    mainClass = 'com.example.demo.core.DemoCoreApplication'

    dependencies {
        implementation project(':demo-db')
    }
}
