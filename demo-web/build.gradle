// 子项目demo-web的build.gradle 文件

// 特定于Spring Boot的配置
springBoot {
    // 主类名
    mainClass = 'com.example.demo.web.DemoWebApplication'

    dependencies {
        implementation 'org.springframework.boot:spring-boot-starter-web'
        implementation project(':demo-core')
    }
}
